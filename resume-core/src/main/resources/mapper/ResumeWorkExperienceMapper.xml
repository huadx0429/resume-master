<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="master.resume.mapper.ResumeWorkExperienceMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="master.resume.model.ResumeWorkExperienceDO">
        <id column="id" property="id" />
        <result column="resume_id" property="resumeId" />
        <result column="company_name" property="companyName" />
        <result column="position" property="position" />
        <result column="work_content" property="workContent" />
        <result column="create_time" property="createTime" />
        <result column="del_flag" property="delFlag" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, resume_id, company_name, position, work_content, create_time, del_flag
    </sql>

</mapper>

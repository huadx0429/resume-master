<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="master.resume.mapper.ResumeEducationMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="master.resume.model.ResumeEducationDO">
        <id column="id" property="id" />
        <result column="resume_id" property="resumeId" />
        <result column="school_name" property="schoolName" />
        <result column="degree" property="degree" />
        <result column="speciality" property="speciality" />
        <result column="start_date" property="startDate" />
        <result column="end_date" property="endDate" />
        <result column="create_time" property="createTime" />
        <result column="del_flag" property="delFlag" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, resume_id, school_name, degree, speciality, start_date, end_date, create_time, del_flag
    </sql>

</mapper>
